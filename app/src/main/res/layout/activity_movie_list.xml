<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.movies.MoviesActivity">


    <com.example.sheraz.utils.widget.CompleteRecyclerView
        android:id="@+id/movie_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        android:clipToPadding="false"
        tools:listitem="@layout/row_movie_list" />

    <!-- Progress and empty views -->
    <include
        layout="@layout/progress_layout_news_article"
        tools:visibility="visible" />

    <include
        layout="@layout/empty_layout_movie_list"
        tools:visibility="invisible" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
